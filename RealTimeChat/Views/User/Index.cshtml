@model AppUser
@if(Model!=null){
<div class="neww">
</div>
<div id="con" class="container rounded bg-white mt-5 mb-5">
    <div class="row">
        <div class="col-md-3 border-right">
            <div class="d-flex flex-column align-items-center text-center p-3 py-5"><img class="rounded-circle mt-5" width="150px" src="@Model.ProfileUrl"><span class="font-weight-bold">@Model.AppUserName</span><span class="text-black-50">@Model.UserName</span><span> <span class="py-3"></span></span></div>
        </div>
        <div class="col-md-5 border-right">
            <div class="p-3 py-5">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h4 class="text-right">Profile Settings</h4>
                </div>
                <div class="row mt-2">
                    <div class="col-md-6"><label class="labels"> User Name :</label><input readonly type="text" asp-for="@Model" class="form-control" value="@Model.AppUserName"></div>

                    <div class="col-md-12"><label class="labels">Email ID</label><input readonly type="text" class="form-control" asp-for="@Model.UserName" value="@Model.UserName"></div>
                </div>
                <div class="row mt-3">
                    


                </div>
                <div class="row mt-3">
                    <div class="col-md-12"><a asp-controller="Order" asp-action="Index" class="form-control text-decoration-none">Order History</a></div>

                </div>
                <div class="mt-5 text-center"><a asp-action="EditProfile" id="saveprof" class="profile-button" type="button">Edit Profile</a></div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="p-3 py-5">
            </div>
        </div>
    </div>
    </div>
}
else
{
    <h3>You are not authorized to view this page</h3>
}
